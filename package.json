{
    "private": true,
    "scripts": {
        "up": "cargo publish --allow-dirty",
        "install": "cargo install --path projects/notedown-lsp --debug",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "push": "wee test && git push",
        "reset": "git reset Head~ --soft",
        "pub": "cargo-publish-all --yes --allow-dirty"
    }
}
